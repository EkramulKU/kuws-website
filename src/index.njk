---
layout: base.njk
title: "Latest Writings | KU Writers' Society"
---

<section class="bg-ku-gold py-3">
  <div class="max-w-7xl mx-auto px-4 flex gap-4">
    <span class="font-bold text-ku-blue">Latest Writings</span>
    <marquee scrollamount="6">
      {% for post in collections.writings | slice(0,7) %}
        🔹 {{ post.data.title }}
      {% endfor %}
    </marquee>
  </div>
</section>

<div class="max-w-6xl mx-auto px-4 py-12 grid md:grid-cols-3 gap-10">

  <!-- LEFT -->
  <div class="md:col-span-2 space-y-12">

    {% set categories = [
      "ফিচার","মতামত","চিঠি-পত্র","কবিতা",
      "গল্প","সাক্ষাৎকার","ভ্রমণ","ধর্ম","বিজ্ঞান ও প্রযুক্তি"
    ] %}

    {% for cat in categories %}
      {% set items = collections.writings | filter("data.type", cat) %}
      {% if items.length > 0 %}
      <section>
        <div class="flex justify-between mb-4">
          <h2 class="text-2xl font-bold">{{ cat }}</h2>
          <a href="/category/{{ cat | url }}/" class="text-sm font-semibold">
            আরও দেখুন →
          </a>
        </div>

        <div class="grid md:grid-cols-2 gap-6">
          {% for post in items | slice(0,4) %}
          <a href="{{ post.url }}"
             class="block bg-white rounded-xl shadow p-5 hover:shadow-lg transition">

            {% if post.data.image %}
            <div class="h-32 overflow-hidden rounded mb-3">
              <img src="{{ post.data.image }}"
                   class="w-full h-full object-cover">
            </div>
            {% endif %}

            <h3 class="font-bold text-lg mb-1">
              {{ post.data.title }}
            </h3>

            <p class="text-sm text-gray-600 mb-2">
              {{ post.data.writer }} | {{ post.data.date }}
            </p>

            <p class="italic text-gray-700">
              “{{ post.data.highlight }}”
            </p>

          </a>
          {% endfor %}
        </div>
      </section>
      {% endif %}
    {% endfor %}

  </div>

  <!-- RIGHT -->
  <aside class="bg-white rounded-xl shadow p-6 h-fit">
    <h3 class="text-xl font-bold mb-4">সর্বশেষ লেখা</h3>
    <ul class="space-y-2 text-sm">
      {% for post in collections.writings | slice(0,7) %}
        <li>• <a href="{{ post.url }}">{{ post.data.title }}</a></li>
      {% endfor %}
    </ul>
  </aside>

</div>
